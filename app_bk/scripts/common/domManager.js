/**
dom要素を渡す
*/
ModuleManager.module("myapp.common.DomManager", function(obj){

	var fragment = document.createDocumentFragment();

	return {


		/*dom要素の変更を反映する処理*/
		bind: function bind(){

		}
	}
});

var dommanager;
var manager = dommanager(obj);

var manager1 = manager.addDiv
var manager2 = manager1.addSpan
manager2.addText

manager.getElement
obj.appendChild(manage.getelement)